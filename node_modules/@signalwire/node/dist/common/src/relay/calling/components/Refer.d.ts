import { BaseComponent } from './BaseComponent';
import { ReferParams } from '../../../util/interfaces';
import Call from '../Call';
export declare class Refer extends BaseComponent {
    call: Call;
    params: ReferParams;
    eventType: string;
    method: string;
    controlId: string;
    referTo: string;
    referResponseCode: string;
    referNotifyCode: string;
    constructor(call: Call, params: ReferParams);
    get payload(): any;
    notificationHandler(params: any): void;
}
